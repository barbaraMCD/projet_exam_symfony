{% extends 'base.html.twig' %}

{% block header %}

{% if user %}
        {% include "./components/header_user.html.twig" %}
        {% else %}
        {% include "./components/header.html.twig"  %}
        {% endif %}
{% endblock %}

{% block content %}

<div class="home">
    <img class="fond" src="{{ asset('images/fond.png') }}" alt="">
    <div class="all-title-home">
        <h1 class="title-home"> Bienvenue sur tous écrivain </h1>
        <span class="sub-title-home"> Une plateforme sociale d'oeuvres en tout genre originale </span>
        <span class="sub-sub-title-home"> Quel est le but ? Cela permet à des auteurs inconnus de publier leurs oeuvres pour les voir apparaître dans de futurs scénarios ou dans le but de les vendre au grand public </span>
    </div>
</div>
{% endblock %}
